#import 'session' class from /main

#Give user option to start a new session

#While user does not 'end' the session, allow them to do other things
#takes in commands to do other things (example: print current time duration)

#if 'end', then show current time
#put the data from ended session into a list (for history)

#let user create a new session